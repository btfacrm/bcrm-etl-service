name: Build
 
on:
  push:
    branches: [ "main" ]
  pull_request: 

jobs: 
  Mulesoft:
    uses: btfacrm/common-devops/.github/workflows/mule-build.yml@main
    secrets: inherit

    # Needed by the Unit Test Action
    permissions:
      contents: write
      issues: write
      checks: write
