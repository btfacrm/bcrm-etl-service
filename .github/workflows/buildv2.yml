name: Build v2
 
on:
  push:
    branches: [ "main" ]
  pull_request: 

jobs: 
  Mulesoft:
    uses: btfacrm/common-devops/.github/workflows/mule-secretsmgm.yml@main
    secrets:
      azure-credentials: inherit
    
    # Needed by the Unit Test Action
    permissions:
      contents: write
      issues: write
      checks: write
